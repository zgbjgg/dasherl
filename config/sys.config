[
  {dasherl, [
    % routes is the pathname (url) and the py file handling that request
    % commonly the file handling the request has a callback and MUST implement
    % the app import for dasherl main py file
    {routes, []}
  ]}
].
